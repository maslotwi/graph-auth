# Empty = same-origin requests via Vite dev proxy (/api -> localhost:8080)
# Set to the backend address when accessing from another machine (e.g. http://192.168.x.x:8080)
VITE_API_BASE_URL=http://localhost:8080
VITE_ISSUER_URL=http://localhost:8080
